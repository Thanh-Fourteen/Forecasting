# dap-an/ — bản đã sửa (KHÔNG vào zip phát học viên)

Cùng tên tệp, cùng chữ ký hàm với `code/`. `python lab.py check --dap-an` phải xanh 6/6.

- `phan_cap.py`: `PHUONG_PHAP = "AutoETS/MinTrace_method-mint_shrink"` (dự báo đem dùng đã khớp; OLS cũng đạt).
- `ve_hinh.py`: sinh `../hinh/*.png` và in mọi con số của tài liệu — chạy trong `lab/`: `python lab.py chay ../dap-an/ve_hinh.py` (~2 phút).
