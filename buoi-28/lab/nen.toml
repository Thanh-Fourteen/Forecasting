# Buổi 28 — Dự báo phân cấp và reconciliation. Nguồn của lab/00-nen/ (python tools/sinh_nen.py 28).
# Không sửa tay bất cứ gì trong lab/00-nen/.

# Thư viện buổi dùng — chỉ ghi TÊN; phiên bản lấy từ tools/nen/phien-ban.toml.
thu_vien = ["matplotlib", "scipy", "statsforecast", "hierarchicalforecast", "rdata", "pyarrow"]

# Bộ dữ liệu — tên trong tools/du-lieu/danh-muc.toml.
du_lieu = ["tourism-australia-tsibble"]

khung_bo = []

[cham]
code_phai_do = true
