# dap-an/ — bản đã sửa (KHÔNG vào zip phát học viên)

Cùng tên tệp, cùng chữ ký hàm với `code/`. `python lab.py check --dap-an` phải xanh 7/7.

- `conformal.py`: `aci` cập nhật $\alpha_{t+1} = \alpha_t + \gamma(\alpha - \text{lỗi}_t)$; `PHUONG_PHAP = "aci"`; γ = 0,005 định trước (bài gốc).
- `ve_hinh.py`: sinh `../hinh/*.png` (150 dpi) và in mọi con số của tài liệu — chạy trong `lab/`: `python lab.py chay ../dap-an/ve_hinh.py` (~80 giây).
