# dap-an/ — bản đã sửa (KHÔNG vào zip phát học viên)

Cùng tên tệp, cùng chữ ký hàm với `code/`. `python lab.py check --dap-an` phải xanh 5/5 (khoảng 3 phút).

- `bayes.py`: `PRIOR = {"mu_sd": 1.0, "tau_sd": 1.0}`; `dung_duoc` đòi r_hat < 1,01, ESS ≥ 400 và 0 divergence; `THANH_PHAN_GP = ("muot", "tuan")`.
- `ve_hinh.py`: sinh `../hinh/*.png` và in mọi con số của tài liệu — chạy trong `lab/`: `python lab.py chay ../dap-an/ve_hinh.py` (~15 phút: GP và BSTS).
