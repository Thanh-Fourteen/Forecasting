# dap-an/ — bản đã sửa (KHÔNG vào zip phát học viên)

Cùng tên tệp, cùng chữ ký hàm với `code/`. `python lab.py check --dap-an` phải xanh 9/9.

- `xac_suat.py`: `quantile_tu_phan_du` lấy quantile sai số ngoài mẫu của 3 tháng trước; `sua_crossing` sắp xếp từng giờ; `crps_mau` đủ hai vế.
- `ve_hinh.py`: sinh `../hinh/*.png` (150 dpi) và in mọi con số của tài liệu — chạy trong `lab/`: `python lab.py chay ../dap-an/ve_hinh.py` (~25 giây).
