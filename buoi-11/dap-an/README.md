# dap-an/ — bản đã sửa (KHÔNG vào zip phát học viên)

Cùng tên tệp, cùng chữ ký hàm với `code/`. `make check BAI=dap-an` phải xanh.
Hình của tài liệu sinh bằng `ve_hinh.py` trong thư mục này → `../hinh/*.png` (150 dpi, tiêu đề nói kết luận).
