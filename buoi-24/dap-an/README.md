# dap-an/ — bản đã sửa (KHÔNG vào zip phát học viên)

Cùng tên tệp, cùng chữ ký hàm với `code/`. `python lab.py check --dap-an` phải xanh.
Hình của tài liệu sinh bằng `ve_hinh.py` trong thư mục này → `../hinh/*.png` (150 dpi, tiêu đề nói kết luận), đồng thời in mọi con số của
tài liệu: `python lab.py chay ../dap-an/ve_hinh.py` trong `lab/` (lần đầu ~5 phút, lưu cache).
